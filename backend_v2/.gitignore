# horibot.db 는 git 추적 (개발 단계 — 캘 데이터 동기화). transient sidecar 만 무시.
*.db-wal
*.db-shm
*.db-journal
autogen_tmp.db
# calibration blob object stores (deployment object_uri)
.calib_blobs_mock/
storage_v2/
