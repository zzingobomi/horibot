# 실행:
#   uv sync
#   uv run --no-sync python -m apps.main --host mock

driver_mode: mock

zenoh:
  mode: peer
  scouting:
    multicast:
      enabled: false

modules:
  - name: motor
    robots: [so101_6dof_0]
  - name: motion
    robots: [so101_6dof_0]
  - name: camera
    robots: [so101_6dof_0]
  - name: camera_decoded
    robots: [so101_6dof_0]
  - name: bridge
