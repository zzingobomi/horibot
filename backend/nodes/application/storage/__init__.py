"""StorageNode — RDB / blob store 의 Zenoh service gateway.

도메인별 handler group:
- handlers/calibration.py (CalibrationHandlers)
- handlers/scan_workflow.py (ScanWorkflowHandlers)

node.py 자리 lifecycle + composition root.
"""
