# horibot.db 는 git 추적 (개발 단계 — 캘 데이터 동기화). transient sidecar 만 무시.
*.db-wal
*.db-shm
*.db-journal
autogen_tmp.db
# calibration blob object stores (deployment object_uri)
.calib_blobs_mock/
storage_v2/
# detect_oriented 디버그 덤프 (modules/detector/module.py _DEBUG_DIR)
debug/
# 중앙 로그 수집 (modules/logcollector — docs/logging.md). 머신별 + 14일 rolling +
# 무한 증가라 push 대상 아님. debug/ 와 분리 (라이프사이클 다름 — 보존 vs scratch).
logs/
