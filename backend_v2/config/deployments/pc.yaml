# 실행:
#   uv sync
#   uv run --no-sync python -m apps.main --host pc

driver_mode: real

zenoh:
  mode: peer
  scouting:
    multicast:
      enabled: true

modules:
  - name: camera_decoded
    robots: [so101_6dof_0]
  - name: bridge # HTTP/WS gateway (frontend_v2 진입점, :8000)
